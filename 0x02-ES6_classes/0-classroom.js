/**
 * Represents a class called room. By Okpako Michael
 */
export default class ClassRoom {
  /**
   * Creates a new @see {@link ClassRoom}.
   * @parameter {Number} maxStudentsSize - The maximum number of
   * students in the class presented.
   */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
